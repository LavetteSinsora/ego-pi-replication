# local LeRobot dataset (fetched from HF Hub on pods)
data/
lerobot_datasets/

__pycache__/
*.pyc

# repo-local venv (uv; see wrist_replay/README.md)
.venv/

# data_extraction recompute cache (self-contained inside the package)
data_extraction/work/

# macOS / MuJoCo noise
.DS_Store
MUJOCO_LOG.TXT

# generated dashboard export (regenerate via data_extraction.dashboard)
episode_*_dashboard.html
